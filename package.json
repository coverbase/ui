{
    "name": "coverbase",
    "private": true,
    "workspaces": [
        "packages/*"
    ],
    "scripts": {
        "build": "npm run build -w packages"
    },
    "devDependencies": {
        "prettier": "^3.0.3",
        "prettier-plugin-tailwindcss": "^0.5.4",
        "typescript": "^5.2.2",
        "unocss": "^0.56.0"
    }
}
